[package]
name = "index_math"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
