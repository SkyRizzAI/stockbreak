/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getFeeKindDecoder,
  getFeeKindEncoder,
  type FeeKind,
  type FeeKindArgs,
} from "../types";

export const FEES_CLAIMED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([22, 104, 110, 222, 38, 157, 14, 62]);

export function getFeesClaimedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FEES_CLAIMED_EVENT_DISCRIMINATOR,
  );
}

export type FeesClaimedEvent = {
  index: Address;
  kind: FeeKind;
  recipient: Address;
  shares: bigint;
};

export type FeesClaimedEventArgs = {
  index: Address;
  kind: FeeKindArgs;
  recipient: Address;
  shares: number | bigint;
};

/** Gets the encoder for {@link FeesClaimedEventArgs} event data. */
export function getFeesClaimedEventEncoder(): FixedSizeEncoder<FeesClaimedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["index", getAddressEncoder()],
      ["kind", getFeeKindEncoder()],
      ["recipient", getAddressEncoder()],
      ["shares", getU64Encoder()],
    ]),
    [getConstantEncoder(FEES_CLAIMED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link FeesClaimedEvent} event data. */
export function getFeesClaimedEventDecoder(): FixedSizeDecoder<FeesClaimedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["index", getAddressDecoder()],
      ["kind", getFeeKindDecoder()],
      ["recipient", getAddressDecoder()],
      ["shares", getU64Decoder()],
    ]),
    [getConstantDecoder(FEES_CLAIMED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link FeesClaimedEvent} event data. */
export function getFeesClaimedEventCodec(): FixedSizeCodec<
  FeesClaimedEventArgs,
  FeesClaimedEvent
> {
  return combineCodec(
    getFeesClaimedEventEncoder(),
    getFeesClaimedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link FeesClaimedEvent} event data. */
export function parseFeesClaimedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): FeesClaimedEvent {
  if (containsBytes(data, FEES_CLAIMED_EVENT_DISCRIMINATOR, 0)) {
    return getFeesClaimedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "FeesClaimedEvent" event discriminators.',
  );
}
