/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/kit/program-client-core";
import { findMarketPda } from "../pdas";
import { MOCK_MARKET_PROGRAM_ADDRESS } from "../programs";
import {
  getPriceInputDecoder,
  getPriceInputEncoder,
  type PriceInput,
  type PriceInputArgs,
} from "../types";

export const SET_PRICES_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  155, 1, 215, 185, 100, 8, 177, 186,
]);

export function getSetPricesDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_PRICES_DISCRIMINATOR);
}

export type SetPricesInstruction<
  TProgram extends string = typeof MOCK_MARKET_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type SetPricesInstructionData = {
  discriminator: ReadonlyUint8Array;
  prices: Array<PriceInput>;
};

export type SetPricesInstructionDataArgs = { prices: Array<PriceInputArgs> };

export function getSetPricesInstructionDataEncoder(): Encoder<SetPricesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["prices", getArrayEncoder(getPriceInputEncoder())],
    ]),
    (value) => ({ ...value, discriminator: SET_PRICES_DISCRIMINATOR }),
  );
}

export function getSetPricesInstructionDataDecoder(): Decoder<SetPricesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["prices", getArrayDecoder(getPriceInputDecoder())],
  ]);
}

export function getSetPricesInstructionDataCodec(): Codec<
  SetPricesInstructionDataArgs,
  SetPricesInstructionData
> {
  return combineCodec(
    getSetPricesInstructionDataEncoder(),
    getSetPricesInstructionDataDecoder(),
  );
}

export type SetPricesAsyncInput<
  TAccountAuthority extends InstructionSignerInput = InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput = InstructionAccountInput,
> = {
  authority: TAccountAuthority;
  market?: TAccountMarket;
  prices: SetPricesInstructionDataArgs["prices"];
};

export async function getSetPricesInstructionAsync<
  TAccountAuthority extends InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MOCK_MARKET_PROGRAM_ADDRESS,
>(
  input: SetPricesAsyncInput<TAccountAuthority, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetPricesInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountAuthority,
      InstructionAccountInputAddress<TAccountAuthority>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMarket,
      InstructionAccountInputAddress<TAccountMarket>
    >
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MOCK_MARKET_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    authority: {
      value: input.authority ?? null,
      isSigner: true,
      isWritable: false,
    },
    market: { value: input.market ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.market.value) {
    accounts.market.value = await findMarketPda({ programAddress });
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("market", accounts.market),
    ],
    data: getSetPricesInstructionDataEncoder().encode(
      args as SetPricesInstructionDataArgs,
    ),
    programAddress,
  } as SetPricesInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountAuthority,
      InstructionAccountInputAddress<TAccountAuthority>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMarket,
      InstructionAccountInputAddress<TAccountMarket>
    >
  >);
}

export type SetPricesInput<
  TAccountAuthority extends InstructionSignerInput = InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput = InstructionAccountInput,
> = {
  authority: TAccountAuthority;
  market: TAccountMarket;
  prices: SetPricesInstructionDataArgs["prices"];
};

export function getSetPricesInstruction<
  TAccountAuthority extends InstructionSignerInput,
  TAccountMarket extends InstructionAccountInput,
  TProgramAddress extends Address = typeof MOCK_MARKET_PROGRAM_ADDRESS,
>(
  input: SetPricesInput<TAccountAuthority, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): SetPricesInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountAuthority,
    InstructionAccountInputAddress<TAccountAuthority>
  >,
  ResolvedInstructionAccountMeta<
    TAccountMarket,
    InstructionAccountInputAddress<TAccountMarket>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? MOCK_MARKET_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    authority: {
      value: input.authority ?? null,
      isSigner: true,
      isWritable: false,
    },
    market: { value: input.market ?? null, isSigner: false, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("market", accounts.market),
    ],
    data: getSetPricesInstructionDataEncoder().encode(
      args as SetPricesInstructionDataArgs,
    ),
    programAddress,
  } as SetPricesInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountAuthority,
      InstructionAccountInputAddress<TAccountAuthority>
    >,
    ResolvedInstructionAccountMeta<
      TAccountMarket,
      InstructionAccountInputAddress<TAccountMarket>
    >
  >);
}

export type ParsedSetPricesInstruction<
  TProgram extends string = typeof MOCK_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    market: TAccountMetas[1];
  };
  data: SetPricesInstructionData;
};

export function parseSetPricesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetPricesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), market: getNextAccount() },
    data: getSetPricesInstructionDataDecoder().decode(instruction.data),
  };
}
